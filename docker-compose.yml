services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: demarca
      POSTGRES_USER: demarca
      POSTGRES_PASSWORD: demarca
    ports:
      - "5432:5432"
    volumes:
      - demarca_db:/var/lib/postgresql/data

volumes:
  demarca_db: